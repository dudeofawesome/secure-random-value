{
  "name": "secure-random-value",
  "version": "0.0.2",
  "description": "Generates cryptographically secure random values",
  "main": "build/index.js",
  "repository": "https://github.com/dudeofawesome/secure-random-value.git",
  "author": "Louis Orleans <louis@orleans.io>",
  "license": "MIT",
  "scripts": {
    "debug": "node --inspect .",
    "lint": "tslint -c tslint.json --project tsconfig.json",
    "pretest": "tsc",
    "test": "yarn run lint && yarn run test:unit && yarn run test:coverage && yarn run test:nsp",
    "test:unit": "mocha build/test",
    "test:nsp": "nsp check",
    "test:coverage": "nyc -x build/test mocha build/test",
    "prepublish": "yarn test && tsc"
  },
  "pre-commit": [
    "lint"
  ],
  "devDependencies": {
    "@types/chai": "^3.4.35",
    "@types/mocha": "^2.2.40",
    "@types/node": "^7.0.12",
    "chai": "^3.5.0",
    "codecov": "^2.1.0",
    "mocha": "^3.2.0",
    "nsp": "^2.6.3",
    "nyc": "^10.2.0",
    "pre-commit": "^1.2.2",
    "tslint": "^4.5.1",
    "typescript": "^2.2.2"
  }
}
